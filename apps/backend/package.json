{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "uvicorn knowledge_assistant.interfaces.api.main:app --reload --app-dir src --host 0.0.0.0 --port 8000",
    "test": "pytest -q",
    "lint": "ruff check src"
  }
}
